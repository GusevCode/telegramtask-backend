const express = require('express');

const router = express.Router();

router.get(':year/:month', );
router.post('/', );
router.patch(':year/:month', );
router.delete(':year/:month');

module.exports = router;